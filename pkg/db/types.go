package db

/*
import (
	"ymir/pkg/api/model"
)

type Datastore interface {
	Create(model model.Model) error
	Update(model model.Model) error
	List(model model.Model) ([]model.Model, error)
	Get(id string) (model.Model, error)
}
*/
